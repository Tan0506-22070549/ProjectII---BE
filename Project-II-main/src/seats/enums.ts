export enum SeatType {
  STANDARD = 'STANDARD',
  VIP = 'VIP',
  COUPLE = 'COUPLE',
  ACCESSIBLE = 'ACCESSIBLE',
}

export enum SeatStatus {
  ACTIVE = 'ACTIVE',     // dùng được
  BLOCKED = 'BLOCKED',   // khoá ghế (hỏng/ẩn)
}
